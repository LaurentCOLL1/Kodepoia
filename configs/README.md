# Configs

Only non-secret configuration templates belong here.

Never store credentials, API tokens, signing secrets or private keys in this directory. Runtime secrets will be brokered by KodeSecrets.
