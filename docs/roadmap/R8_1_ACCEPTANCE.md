# R8.1 — Asset/Vault contracts, identity, schemas + boundary — Acceptance

**Status:** CANDIDATE / PENDING EXACT-HEAD CI  
**Manual intervention:** NONE

The candidate implements the frozen R8.1 scope: typed contracts, deterministic logical/revision identities, v1 manifests, canonical/tamper-checked deserialization, exact content verification and an explicitly rooted Vault boundary composed from `WorkspaceBoundary` semantics.

Acceptance requires R0 Repository Guard, full Python Core and KodeStudio UI Smoke SUCCESS on the exact implementation head, plus the focused R8.1 schema/identity/hash/path tests. Final accepted SHA, run IDs and test counts are recorded only after hosted evidence succeeds; no success is inferred from this candidate document.
