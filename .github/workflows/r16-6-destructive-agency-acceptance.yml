name: R16.6 Destructive Agency Acceptance

on:
  push:
    branches:
      - r16/06-destructive-command-excessive-agency-hardening
  pull_request:
    paths:
      - .github/workflows/r16-6-destructive-agency-acceptance.yml
      - scripts/r16_6_destructive_agency_acceptance.py
      - src/kodepoia/core/destructive_guard.py
      - src/kodepoia/core/guardian.py
      - tests/test_r16_destructive_guard.py
      - tests/test_guardian.py
      - docs/roadmap/R16_PLAN.md
      - docs/continuity/KODEPOIA_CONTINUITY.md
  workflow_dispatch:

permissions:
  contents: read

jobs:
  destructive-agency:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    env:
      EVIDENCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
    steps:
      - name: Checkout exact evidence source
        uses: actions/checkout@v4
        with:
          ref: ${{ env.EVIDENCE_SHA }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Assert exact checkout provenance
        shell: python
        run: |
          import os
          import subprocess

          expected = os.environ["EVIDENCE_SHA"].strip().lower()
          actual = subprocess.check_output(
              ["git", "rev-parse", "HEAD"], text=True, encoding="utf-8"
          ).strip().lower()
          if actual != expected:
              raise SystemExit(f"checkout mismatch: expected {expected}, got {actual}")

      - name: Install focused acceptance dependencies
        run: python -m pip install -e ".[dev]"

      - name: Compile focused R16.6 sources
        run: >-
          python -m compileall -q
          src/kodepoia/core/destructive_guard.py
          src/kodepoia/core/guardian.py
          scripts/r16_6_destructive_agency_acceptance.py
          tests/test_r16_destructive_guard.py
          tests/test_guardian.py

      - name: Ruff focused R16.6 sources
        run: >-
          python -m ruff check
          src/kodepoia/core/destructive_guard.py
          src/kodepoia/core/guardian.py
          scripts/r16_6_destructive_agency_acceptance.py
          tests/test_r16_destructive_guard.py
          tests/test_guardian.py

      - name: Run focused R16.6 tests
        run: >-
          python -m pytest -q
          tests/test_r16_destructive_guard.py
          tests/test_guardian.py

      - name: Emit exact-source R16.6 acceptance
        run: >-
          python scripts/r16_6_destructive_agency_acceptance.py
          --source-sha "${{ env.EVIDENCE_SHA }}"
          --output artifacts/r16_6_destructive_agency_acceptance.json

      - name: Upload exact-source acceptance artifact
        uses: actions/upload-artifact@v4
        with:
          name: r16-6-destructive-agency-${{ runner.os }}-${{ env.EVIDENCE_SHA }}
          path: artifacts/r16_6_destructive_agency_acceptance.json
          if-no-files-found: error
          retention-days: 30
