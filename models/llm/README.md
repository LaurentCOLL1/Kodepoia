# LLM models

Local language-model payloads. Prefer role-oriented subfolders such as `fast/`, `core/`, `coder/`, `reviewer/`, `vision/` and `general/`; provider/runtime identity belongs in each tracked manifest rather than in the top-level taxonomy.
