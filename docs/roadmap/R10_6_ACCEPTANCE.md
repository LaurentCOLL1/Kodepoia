# R10.6 — Acceptance record

Status: **IMPLEMENTATION CANDIDATE; HOSTED EXACT-HEAD GATES PENDING**  
Manual intervention: **CONDITIONAL — decision after hosted gates**

## Definition of Done

R10.6 requires exact-head R0 Repository Guard + full Python Core + KodeStudio UI Smoke, deterministic rig-profile identity, hierarchy/deform-set validation, influence/tolerance budgets, malformed weight fixtures, explicit exported-deform set, staging lineage and a real Blender 5.2 deformation fixture if the frozen CONDITIONAL boundary is triggered.

## Hosted evidence required first

Tests must prove default four-influence policy and explicit higher-count opt-in; parent-before-child/connected-rest invariants; control-only weight rejection; deterministic tiny-weight pruning/normalization; create vs imported-existing strategy constraints; PASS/WARN/BLOCK validator behavior for zero weights, bad sums, over-budget influences, control references, modifier/parent binding and deformation probe; immutable source + derived SHA lineage; protocol tamper rejection; static offline bootstrap without Blender auto-weight/paint/network/dynamic-code surface; valid schemas and bounded local-acceptance runner.

## CONDITIONAL decision

The implementation's required deformation probe evaluates the real Blender Armature modifier after applying a temporary pose. Hosted unit tests can validate the protocol and deterministic rules but cannot authoritatively execute Blender 5.2. Therefore, if the hosted implementation head passes all three required gates, mark the condition **TRIGGERED** and stop before R10.7. Run `scripts/r10_6_local_acceptance.py` on that immutable candidate with the local Blender 5.2 executable. The evidence must report `status=pass`, no blockers, accepted Blender 5.2 runtime and a PASS deformation rule. Manual weight painting is forbidden as acceptance evidence.

After reviewed local evidence is bound to the immutable candidate, update this file with its canonical SHA-256/bytes and facts, rerun the three gates on the final documented head, merge with expected SHA, then perform one continuity-only normalization. Only then may R10.7 start.
