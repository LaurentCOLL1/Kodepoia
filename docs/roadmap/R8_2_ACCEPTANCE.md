# R8.2 — Inter-project Vault store, revisions, reuse + preservation — Acceptance

**Status:** CANDIDATE / PENDING EXACT-HEAD CI  
**Manual intervention:** NONE

Candidate scope: content-addressed immutable objects, canonical manifests, rebuildable SQLite index, authorized-project ingest, exact verification, distinct provenance on deduplicated bytes, project references, materialization, preservation/pinning, two-phase deletion and index rebuild.

Final accepted SHA, hosted workflow IDs and suite counts are recorded only after all R0 / full Python Core / KodeStudio UI Smoke gates succeed on one exact candidate head.
