# R13.9 — iOS/iPadOS SwiftUI/Xcode deterministic scaffold — Acceptance

## Frozen acceptance claim

R13.9 is accepted only if one exact implementation head proves all of the following without production signing/account material:

1. Project DNA iOS intent maps to a deterministic native SwiftUI/Xcode definition.
2. Identical definition + shared app model + lineage produces identical generated bytes and workspace manifest.
3. Generated workspace includes a valid Xcode project/shared scheme, SwiftUI app/view/state projection, asset catalog, Info.plist and localization catalogs.
4. Deployment target determines Observation vs `ObservableObject` compatibility truthfully.
5. Project/bundle/path/scheme/destination/build-setting substitution attempts fail closed.
6. Renderer launches no external process and preserves the accepted generated/user-owned file policy.
7. Godot iOS export remains a metadata bridge owned by R5 rather than a second exporter.
8. A hosted `macos-26` job discovers the generated scheme and successfully builds the canonical fixture with the accepted Xcode toolchain for `generic/platform=iOS Simulator`.
9. The hosted invocation has signing disabled by fixed repository-owned argv; evidence contains no Apple account, private key, certificate identity, provisioning profile, team ID, password or App Store Connect token.
10. Simulator evidence does not claim physical-device behavior.

Official Apple references used by this acceptance boundary:

- SwiftUI Observation support and model-data guidance: https://developer.apple.com/documentation/swiftui/managing-model-data-in-your-app
- pre-iOS/iPadOS 17 `ObservableObject` compatibility guidance: https://developer.apple.com/documentation/swiftui/monitoring-model-data-changes-in-your-app
- Simulator/device run-destination separation: https://developer.apple.com/documentation/xcode/running-your-app-on-simulated-or-physical-devices
- command-line generic Simulator destination example: https://developer.apple.com/documentation/xcode/creating-a-multi-platform-binary-framework-bundle

## Required exact-head gates

The final accepted technical candidate must have SUCCESS on the same SHA for:

- R0 Repository Guard;
- full Python Core;
- KodeStudio UI Smoke;
- R13 Apple Xcode Acceptance (R13.8 seam remains healthy);
- R13 Apple SwiftUI Scaffold Acceptance.

Any byte change after those runs invalidates them for merge acceptance.

## Focused tests

`tests/test_r13_9_ios_swiftui_scaffold.py` covers:

- strict schemas;
- byte/semantic determinism;
- expected Xcode/SwiftUI/resources/localizations/shared-contract files;
- iOS 17+ Observation mapping;
- pre-iOS-17 `ObservableObject` compatibility;
- invalid bundle and deployment-version intent;
- Godot bridge partitioning;
- context escaping;
- no subprocess during render;
- user-owned preservation and tamper conflict;
- symlink escape rejection;
- fixed Simulator argv and rejection of injected scheme/staging paths;
- manifest lineage binding.

## Hosted evidence collector

`scripts/r13_9_apple_swiftui_acceptance.py`:

- requires macOS and an exact 40-hex source SHA;
- renders/applies the canonical project in runner-owned temporary storage;
- validates `xcodebuild`/`xcrun` through the R13 mobile boundary;
- probes Xcode and iPhoneSimulator SDK versions;
- verifies the shared scheme through `xcodebuild -list -json`;
- runs only the fixed no-signing Simulator build argv;
- requires the generated `.app` executable to exist;
- validates the resulting JSON against `schemas/r13/apple-swiftui-build-evidence.schema.json`;
- emits no temporary filesystem paths or signing/account material in durable evidence.

## Manual decision

Initial state: **CONDITIONAL / NOT TRIGGERED**.

If the hosted macOS workflow proves the frozen Simulator compile claim, no user action is required. If accepted hosted CI cannot provide the required Simulator/Xcode semantic, R13.9 becomes `MANUAL_REQUIRED`, later subdivisions stop, and one bounded collector/action set must be provided. Production Apple credentials are never a substitute for a missing core Simulator proof.

## Closure sequence

After an accepted technical candidate exists:

1. update `R13_PLAN.md` + continuity in the same end-sync work cycle, marking R13.9 COMPLETE and R13.10 PLANNED;
2. rerun every required gate on that new exact head;
3. merge the implementation PR with `expected_head_sha`;
4. create exactly one continuity-only normalization branch from the merge;
5. require exact-head R0 + Python + UI on the normalization head;
6. merge normalization;
7. only then may R13.10 start.
