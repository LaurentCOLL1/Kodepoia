# Schemas

Versioned machine-readable schemas used by KodeSchema live here.

R0 provides the architecture freeze schema. Project DNA, permissions, tools, memory, plugins and save schemas are added in their implementation phases.
