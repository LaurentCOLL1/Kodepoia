# R8.4 — Duplicate + near-duplicate detection — Acceptance

**Status:** CANDIDATE / PENDING EXACT-HEAD CI  
**Manual intervention:** NONE

Candidate scope: exact SHA-256 groups, typed/versioned fingerprints, deterministic document similarity, Pillow-based image dHash evidence, explicit thresholds and durable non-destructive decisions. Exact-byte dedup preserves distinct logical provenance and near matches never auto-merge.

Final accepted SHA, PR/merge, workflow IDs and suite counts are recorded only after R0, full Python Core and KodeStudio UI Smoke all succeed on the same exact candidate head.
