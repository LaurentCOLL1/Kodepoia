# R8.3 — Source/derived lineage + reproducible transform cache/rebuild — Acceptance

**Status:** CANDIDATE / PENDING EXACT-HEAD CI  
**Manual intervention:** NONE

Candidate scope: typed transform/tool/determinism contracts, registered adapters only, exact-input/tool/environment cache keys, verified cache hit/miss handling, source/derived lineage, cycle rejection, managed staging/promotion, cancellation safety and deterministic pure-Python rebuild evidence.

Final accepted SHA, PR/merge, workflow IDs and suite counts are recorded only after R0, full Python Core and KodeStudio UI Smoke all succeed on the same exact candidate head.
