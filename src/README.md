# Source tree

R0 creates only package/module boundaries. Concrete language/framework choices are made in implementation ADRs rather than encoded prematurely in the architecture.

Major areas: `kodestudio`, `orchestrator`, `brain`, protected `core`, `intelligence`, tool adapters, quality modules and model management.
